# `xxscreeps` is an optional peer dependency provided by the host server at
# runtime — it must never be installed into this package. Without this, pnpm's
# auto-install-peers would pull the entire xxscreeps dependency tree (isolated-vm,
# pathfinder, ...) into node_modules and the lockfile, bloating installs and CI.
auto-install-peers=false
